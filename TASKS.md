# TASKS.md — кто что делает

Старт 13:00, сдача 18:00. Контекст, контракт и правила — в AGENTS.md.
Промпты ниже вставляйте в Codex по одному: одна задача = один маленький коммит или PR.
Личные перерывы (60 минут на человека) — только до 17:00 и не вдвоём одновременно.

## Контрольные точки

| Время | Что должно работать |
|---|---|
| 13:35 | Скелет в main, все сделали `git pull` |
| 13:45 | Первый коммит от каждого |
| 14:40 | **Чекпоинт 1:** черновик → вопросы → карточка → подтверждение в UI (ИИ может быть заглушкой) |
| 15:45 | **Чекпоинт 2:** реальный ИИ, рейтинг с разбивкой, публикация, каталог с позицией |
| 16:45 | **Чекпоинт 3:** демо-сценарий проходит целиком. Feature freeze |
| 17:00 | Последний час: никто не выходит |
| 17:15 | Первая сдача на платформе |
| 17:30 | Два прогона демо |
| 17:45 | Финальный пуш и обновление сдачи |

На каждом чекпоинте синк на 2 минуты. Застрял дольше 20 минут — сразу говоришь команде.

---

## Лид — API, интеграция, README, демо

**L1 · 13:15–13:35 · Скелет.** Сначала закоммить AGENTS.md и TASKS.md в корень, потом:
```text
Прочитай AGENTS.md целиком. Сделай скелет backend строго по §3–§6:
1. backend/requirements.txt: fastapi, uvicorn[standard], sqlmodel, pydantic>=2, python-dotenv, openai, httpx, pytest.
2. backend/.env.example из §5, загрузка .env через python-dotenv.
3. backend/app/schemas.py — Pydantic-модели ровно по контракту §6 (те же имена полей и значения enum).
4. backend/app/db.py и models.py — SQLite через SQLModel: таблицы Task, Team, Proposal. Списки и словари (questions, answers, card, evidence, removed, draft_rating, rating, rating_history, interests, skills, technologies) храни в JSON-колонках.
5. backend/app/main.py — FastAPI, CORS для http://localhost:5173, роутеры, создание таблиц на старте.
6. backend/app/routers/tasks.py, catalog.py, proposals.py, teams.py — все эндпоинты из §6 с правильными схемами ответов, логика пока минимальная.
7. Временные заглушки, которые потом заменит Зарип (сигнатуры из §7 и §8 не меняй): backend/app/rating.py (compute_rating возвращает total=0, level="draft", пустые списки) и backend/app/ai/__init__.py (analyze_draft возвращает 3 шаблонных вопроса из §8, build_card переносит ответы в поля своих вопросов).
8. Дополни корневой .gitignore: .env, .venv, __pycache__, *.db, node_modules, dist.
Проверь: uvicorn app.main:app --reload стартует, /docs показывает все эндпоинты, POST /api/tasks возвращает Task с тремя вопросами. Не трогай frontend/.
```

**L2 · 13:35–14:30 · Флоу задачи**
```text
Прочитай AGENTS.md (§6–§8). Реализуй флоу задачи в backend/app/routers/tasks.py (сервисный слой можно вынести в backend/app/services/tasks.py):
- POST /api/tasks: создать задачу, вызвать ai.analyze_draft, сохранить card, evidence, removed, questions, ai_mode; draft_rating = compute_rating(card); status = clarifying.
- POST /api/tasks/{id}/answers: вызвать ai.build_card, сохранить card, evidence, removed, answers, ai_mode; status = card_ready.
- POST /api/rating/preview: вернуть compute_rating(card), ничего не сохранять.
- PUT /api/tasks/{id}/card: ручное подтверждение. Валидация из §6, сохранить карточку, rating = compute_rating, добавить {total, at} в rating_history, status = confirmed (если была published — остаётся published).
- POST /api/tasks/{id}/publish: только из confirmed, иначе 400 с понятным detail; проставить published_at.
- GET /api/tasks и GET /api/tasks/{id}: с position по правилу §6 и proposals_count.
Сбой ИИ не должен давать 500. Напиши backend/tests/test_flow.py: при AI_MODE=stub проходит черновик → ответы → подтверждение → публикация, у задачи есть rating и position. Не трогай frontend/, backend/app/ai/, rating.py.
```

**L3 · 14:30–15:30 · Каталог, отклики, выбор, seed**
```text
Прочитай AGENTS.md (§6, §10). Реализуй:
- GET /api/catalog?industry=&level=: только published, сортировка и position по §6, needs_clarification = (level == "draft"), need_short — первые 140 символов need.
- GET /api/industries (список из §10), GET /api/examples/drafts (из backend/seed/drafts.json), GET /api/teams.
- POST /api/tasks/{id}/proposals: только для published (иначе 400), валидация из §6. GET /api/tasks/{id}/proposals.
- POST /api/proposals/{id}/decision: только "selected" или "rejected", просто меняет статус. Никакой автоматики.
- Загрузка seed при старте, если БД пустая: teams.json, cards.json как опубликованные задачи (рейтинг через compute_rating), proposals.json. Если seed-файлов ещё нет — старт без ошибок.
Добавь в test_flow.py: отклик на опубликованную задачу и решение бизнеса. Не трогай frontend/, backend/app/ai/, rating.py, seed/.
```

**L4 · 15:30–16:30 · Валидация, ошибки, stretch**
```text
Прочитай AGENTS.md. Пройди по всем эндпоинтам: понятные 400/404/422 с detail на русском, никаких 500 при сбое ИИ, логирование ошибок. Добавь stretch из §6 и §9: POST /api/proposals/{id}/milestone (только для selected: milestones_confirmed += 1, команде +10 points) и GET /api/teams/{id}/recommendations через recommend() из backend/app/recommend.py (если файла ещё нет — пропусти). Прогони pytest и исправь падения в своей зоне. Не трогай frontend/.
```

**L5 · 16:30–17:15 · README и сдача**
```text
Проанализируй текущий проект и создай README.md на русском для жюри хакатона. Используй только то, что подтверждается репозиторием, ничего не придумывай. Разделы:
1. Название и краткое описание: какую проблему решает и для кого.
2. Что реализовано — обязательные модули кейса списком с ✅: конструктор задачи, карточка задачи, рейтинг задачи, общий каталог, отклики команд, выбор бизнеса (и stretch, если есть).
3. Сквозной сценарий от черновика до выбора команды.
4. Формула рейтинга: таблица 7 категорий с весами и проверками, уровни готовности (по backend/app/rating.py).
5. Правила каталога: сортировка и позиция, уровни, низкий рейтинг не скрывает задачу и не запрещает отклик, рекомендации только от 40 баллов, выбор команды только вручную.
6. AI-функция: промпты (из backend/app/ai/prompts.py), формат входа и выхода, защита от выдуманных данных (evidence + guard), обработка некорректного ответа (повтор, цепочка OpenAI → NVIDIA → заглушка), режим AI_MODE=stub. Примеры из docs/ai_examples.md.
7. Технологии и архитектура: компоненты и как они взаимодействуют (схема mermaid).
8. Установка и запуск: пошагово, backend и frontend; без API-ключа работает режим заглушки.
9. Как проверить: тестовые сценарии пошагово (демо-сценарий из AGENTS.md §11) и pytest.
10. Данные и интеграции: seed-наборы и их объём, внешние API.
11. Ограничения текущей версии.
```
Потом: клонируешь репо в чистую папку и запускаешь строго по README. К 17:15 — первая сдача на платформе («Сдать решение»), дальше только обновляешь.

**L6 · 17:15–17:45 · Демо.** Два прогона по docs/demo_script.md (готовит Зарип), финальный пуш, обновление сдачи.

---

## Зарип — рейтинг, ИИ, seed

**Z0 · 13:05–13:35 · Prompt lab, вне репо.** Проверь ключи OpenAI и NVIDIA. На слабом черновике из §11 добейся JSON в формате §8 с 3–5 уместными вопросами. Выбери модель, которая отвечает быстрее 5 секунд. До пуша скелета (~13:35) в репо ничего не создавай.

**Z1 · 13:35–14:05 · Движок рейтинга**
```text
Прочитай AGENTS.md (§6, §7). Твоя зона: backend/app/rating.py и backend/tests/test_rating.py. Замени заглушку compute_rating(card: TaskCard) -> Rating реализацией строго по §7: детерминированно, без LLM, сигнатуру не меняй. У каждой проверки label, field, points, passed и hint с «+N баллов» для непройденной. missing — непройденные проверки по убыванию points. level и next_level по порогам 40/70/90, вынеси level_for(total). Тесты: пустая карточка = 0 и draft; полная качественная карточка = 100 и priority; границы 39/40/69/70/89/90; success_criteria без цифр теряет 7 баллов; contact без email, телефона и @ не проходит. Не трогай другие файлы.
```

**Z2 · 14:05–14:25 · Seed-данные**
```text
Прочитай AGENTS.md (§6, §7, §10). Создай backend/seed/drafts.json, cards.json, teams.json, proposals.json строго по §10: на русском, компании и люди вымышленные, контакты вида name@example.com. Подбери карточки так, чтобы compute_rating давал примерно 94, 81, 66, 48 и 25, и запиши точное значение в expected_score. Одна из команд — DataCats (анализ данных, Python). Добавь в backend/tests/test_rating.py тест: для каждой карточки из cards.json compute_rating(card).total == expected_score. Не трогай другие файлы.
```

**Z3 · 14:25–15:30 · AI-слой**
```text
Прочитай AGENTS.md (§2, §6, §8). Твоя зона: backend/app/ai/. Замени заглушку полноценной реализацией, сигнатуры analyze_draft и build_card не меняй:
- schemas.py: Pydantic-модели ответа модели из §8.
- client.py: цепочка OpenAI → NVIDIA Build → stub по §8 (AI_MODE, ключи и модели из .env, таймаут 20 с, json.loads → Pydantic → один повтор → следующий провайдер, logging.warning).
- prompts.py: системные и пользовательские промпты для analyze и build по правилам §8. Передавай в промпт описание 10 полей карточки и веса рейтинга, чтобы вопросы шли по самым весомым пустым полям.
- guard.py: проверка evidence по §8, отброшенные поля — в removed.
- stub.py: шаблонные вопросы и перенос ответов по §8.
- Вопросов всегда 3–5: добивка шаблонами по missing из compute_rating; id и points проставляет код.
Тесты в backend/tests/test_ai.py: guard отбрасывает поле с выдуманной цитатой и оставляет поле с настоящей; в stub-режиме analyze_draft даёт ≥ 3 вопросов; невалидный JSON от провайдера (замокать) приводит к fallback, а не к исключению. Не трогай другие файлы.
```

**Z4 · 15:30–16:15 · Качество ИИ**
```text
Напиши backend/scripts/ai_check.py и прогони analyze_draft и build_card на всех 5 черновиках из backend/seed/drafts.json с реальным ключом. Для каждого проверь: вопросы уместные и опираются на детали черновика; в карточке нет фактов, которых нет во вводе; guard отбрасывает выдумки. Поправь prompts.py. Сохрани 2 удачных примера «вход → выход» в docs/ai_examples.md. Не трогай файлы вне backend/app/ai/, backend/scripts/, docs/.
```

**Z5 · 16:15–16:45 · Рекомендации (stretch)**
```text
Прочитай AGENTS.md (§9). Реализуй backend/app/recommend.py: recommend(team, tasks) -> list[Recommendation] строго по §9 и тест backend/tests/test_recommend.py: задача с рейтингом < 40 не рекомендуется, reasons содержат совпавшие слова. Роут подключит лид. Не трогай другие файлы.
```

**Z6 · 16:45–17:15 · Сценарий демо.** docs/demo_script.md: точный текст черновика, ответы на ожидаемые вопросы, правки для роста рейтинга, текст отклика DataCats (идея, план, срок, ссылка). Прогони на реальном seed и подгони так, чтобы было ≈65 → ≈90 и место #4 → #2. Передай лиду тексты для раздела README про ИИ.

---

## Аслан — фронтенд

**A0 · 13:05–13:40 · Каркас**
```text
Прочитай AGENTS.md. Твоя зона — только frontend/. Создай приложение React + Vite + TypeScript с Ant Design 5 (ConfigProvider с локалью ru_RU) и react-router-dom. В vite.config.ts proxy: /api → http://localhost:8000.
- src/api/types.ts — интерфейсы ровно из §6 AGENTS.md.
- src/api/client.ts — функции для всех эндпоинтов §6 на fetch; при ошибке показывать detail через message.error.
- Layout: шапка «TaskReady» и переключатель роли: «Бизнес» или «Команда» с выбором команды из GET /api/teams. Роль храни в React context.
- Роуты: / (Каталог), /tasks/new (Конструктор), /tasks/:id (Задача), /my (Мои задачи) — пока пустые страницы с заголовками.
Проверь, что npm run dev запускается без ошибок. Не трогай backend/.
```

**A1 · 13:40–14:40 · Конструктор**
```text
Прочитай AGENTS.md (§6, §11). Сделай страницу /tasks/new для роли «Бизнес» с Ant Design Steps: Черновик → Уточнение → Карточка → Публикация.
1. Черновик: TextArea (минимум 20 символов), Select отрасли из GET /api/industries, кнопка «Взять пример» (GET /api/examples/drafts), кнопка «Проанализировать» → POST /api/tasks со спиннером.
2. Уточнение: «Оценка черновика: N/100» из draft_rating. Список вопросов: текст, тег поля по-русски, бейдж «+N баллов», поле ответа. Кнопка «Сформировать карточку» → POST /api/tasks/{id}/answers.
3. Карточка: форма из 10 полей TaskCard с русскими подписями. Под полем, заполненным ИИ, серым: «Из вашего текста: «…»» (evidence). Если removed не пуст — Alert «ИИ не стал заполнять: …». Справа прогноз рейтинга: POST /api/rating/preview с debounce 400 мс при каждом изменении, подпись «прогноз, засчитывается после подтверждения» (пока число и уровень, панель будет в A2). Кнопка «Подтвердить карточку» → PUT /api/tasks/{id}/card.
4. Публикация: подтверждённый рейтинг и кнопка «Опубликовать» → POST /api/tasks/{id}/publish → «Задача в каталоге на месте #N» и ссылка на страницу задачи.
Везде показывай тег ai_mode: «ИИ: OpenAI», «ИИ: NVIDIA» или «ИИ: заглушка». Не трогай backend/.
```

**A2 · 14:40–15:45 · Панель рейтинга и каталог**
```text
Прочитай AGENTS.md (§6, §7). Сделай компонент src/components/RatingPanel.tsx (props: rating: Rating, preview?: boolean):
- большой Progress type="circle" с total и Tag уровня: draft — серый «Черновик · требует уточнения», working — синий «Рабочая», ready — зелёный «Готовая», priority — золотой «Приоритетная»;
- полоса 0–100 с отметками 40, 70 и 90 и строка «До уровня «…» не хватает N баллов» из next_level;
- 7 категорий: earned/max и проверки с галочками;
- блок «Что повысит рейтинг» из missing с бейджами «+N»;
- если preview — подпись «прогноз».
Подключи его в конструкторе вместо временного блока.
Сделай страницу / Каталог: GET /api/catalog, фильтры по отрасли и уровню (Select). Задачи списком по порядку: #position, название, отрасль, business_name, need_short, круг рейтинга, Tag уровня, число откликов. У priority — выделенная рамка, у draft — пометка «требует уточнения». Клик ведёт на /tasks/:id. Не трогай backend/.
```

**A3 · 15:45–16:45 · Страница задачи, отклики, выбор**
```text
Прочитай AGENTS.md (§6, §11). Сделай страницу /tasks/:id:
- все поля карточки, RatingPanel подтверждённого рейтинга, история рейтинга строкой («48 → 65 → 90»), место в каталоге.
- Роль «Бизнес»: кнопка «Редактировать» → та же форма карточки с прогнозом → «Подтвердить» (PUT /card) → уведомление «Рейтинг 65 → 90, место #4 → #2» (старые значения запомни до сохранения).
- Роль «Бизнес»: отклики (GET /api/tasks/{id}/proposals): команда, идея, план, срок, ссылка, статус. Кнопки «Выбрать» и «Отклонить» (POST /api/proposals/{id}/decision) с Popconfirm. Можно выбрать несколько.
- Роль «Команда»: форма «Откликнуться» (idea, plan, deadline, prototype_url) → POST /api/tasks/{id}/proposals. Если уровень draft — Alert «Задача требует уточнения, но откликнуться можно».
Сделай страницу /my: таблица всех задач (GET /api/tasks): название, статус, рейтинг, уровень, отклики, ссылка. Не трогай backend/.
```

**A4 · 16:45–17:15 · Полировка**
```text
Пройди демо-сценарий из §11 AGENTS.md целиком в браузере с запущенным backend и исправь всё, что ломается во frontend/: состояния загрузки и пустые состояния, ошибки через message.error, никаких ошибок в консоли. Контракт не меняй. Не трогай backend/.
```

Если останется время: кнопка «Подтвердить этап» у выбранного отклика и баллы команды в шапке; блок «Рекомендовано вашей команде» в каталоге для роли «Команда» (GET /api/teams/{id}/recommendations, с причинами).

---

## Если отстаём

- В 15:45 A2 не готов → лид берёт из A3 отклики с выбором и страницу /my (отдельные файлы `frontend/src/components/Proposals.tsx` и `frontend/src/pages/MyTasks.tsx`, договориться с Асланом).
- ИИ нестабилен → на демо AI_MODE=stub. Промпт, формат и обработка ошибок всё равно описаны в README, кейс это разрешает.
- Режем в таком порядке: этапы и баллы команд → рекомендации → /my → история рейтинга.
- Не режем никогда: вопросы, карточку с подтверждением, рейтинг с разбивкой, каталог с позицией, отклик, ручной выбор.
