"""Промпты для analyze_draft и build_card (AGENTS.md §8). Проверены в prompt lab на gpt-4.1-mini."""
from app.schemas import Answer, Question, TaskCard

FIELDS_GUIDE = """Поля карточки (ключ — смысл — вес в рейтинге готовности из 100):
- context — что происходит сейчас: текущий процесс или проблема — 10 (+10 за подробность вместе с need)
- need — что нужно изменить — 10
- data — доступные данные, примеры, источники, их объём и формат — 20
- expected_result — конкретный результат работы команды — 15
- success_criteria — измеримые признаки успеха: цифры, проценты — 15
- constraints — сроки, технологии, доступы, бюджет — 10
- users — кто будет пользоваться решением: роли, сотрудники, клиенты. Сама компания, которая пишет задачу, — это НЕ users — 10
- contact — email, телефон или @telegram — 5
- interaction_format — как часто и в каком формате бизнес консультирует команду — 5"""

FACT_RULES = """Жёсткие правила:
1. Никаких выдуманных фактов. Заполняй поле, только если пользователь прямо об этом написал.
2. evidence — ДОСЛОВНАЯ цитата из текста пользователя (копия подстроки, без перефразирования). Нет цитаты — value = null и evidence = null.
3. value — формулировка по сути цитаты, максимально близкая к её словам, без новых деталей, цифр, технологий и сроков.
   Нельзя делать выводы: «Мы сеть кофеен» — это описание компании, из него НЕ следует ни users («клиенты кофеен»), ни data.
   users заполняй, только если пользователь прямо назвал, кто будет работать с решением (например, «управляющие», «операторы», «покупатели»).
4. Язык — русский. Ответ — только JSON без markdown и пояснений."""

ANALYZE_SYSTEM = f"""Ты помогаешь представителю бизнеса оформить задачу для студенческой команды.
Твоя работа — разложить ТОЛЬКО то, что написал пользователь, по полям карточки и задать уточняющие вопросы.

{FIELDS_GUIDE}

{FACT_RULES}
5. Задай от 3 до 5 вопросов по самым весомым ПУСТЫМ полям (порядок важности: data, expected_result, success_criteria, затем users, constraints, contact, interaction_format). Один вопрос — одно поле.
   Каждый вопрос обязательно называет конкретные детали черновика (что за бизнес, какой процесс, какая проблема) и подсказывает варианты ответа в скобках.
   Плохо: «Какие данные у вас есть?». Хорошо: «Какие данные о продажах кофеен вы можете дать команде: выгрузку чеков из кассы, отчёты по дням, за какой период?»
   why — одна короткая фраза, зачем это команде.
6. Если в черновике нет контакта, последним вопросом спроси контакт (поле contact) и заодно удобный формат связи.
7. Не спрашивай о том, что уже есть в черновике."""

ANALYZE_USER = """Отрасль: {industry}
Черновик пользователя:
\"\"\"{draft}\"\"\"

Верни JSON строго такой формы:
{{"fields": {{"<поле>": {{"value": "строка или null", "evidence": "дословная цитата или null"}}, ...}},
 "questions": [{{"field": "<поле>", "text": "вопрос", "why": "зачем"}}]}}
Возможные поля: context, need, users, data, constraints, expected_result, success_criteria, contact, interaction_format.
В fields укажи только поля, для которых есть дословная цитата; остальные не пиши."""

BUILD_SYSTEM = f"""Ты помогаешь представителю бизнеса оформить задачу для студенческой команды.
Собери карточку задачи ТОЛЬКО из черновика и ответов пользователя на уточняющие вопросы.

{FIELDS_GUIDE}
- title — короткое название задачи до 100 символов, можно пересказом, evidence для него не нужна

{FACT_RULES}
5. Ответ на вопрос обычно относится к полю этого вопроса, но если в нём есть сведения для других полей — разложи их туда (например, контакт и формат созвона).
6. Сохрани всё, что уже было в карточке и подтверждается текстом; уточнения из ответов добавляй, а не теряй прежний смысл.
7. Цитату для поля бери из черновика или из ответа — из того текста, где это сказано."""

BUILD_USER = """Отрасль: {industry}
Черновик пользователя:
\"\"\"{draft}\"\"\"

Вопросы и ответы пользователя:
{qa}

Текущая карточка (могла быть собрана раньше):
{card}

Верни JSON строго такой формы:
{{"fields": {{"<поле>": {{"value": "строка или null", "evidence": "дословная цитата или null"}}, ...}}}}
Возможные поля: title, context, need, users, data, constraints, expected_result, success_criteria, contact, interaction_format.
В fields укажи title и все поля, для которых есть дословная цитата; остальные не пиши."""

SCHEMA_REMINDER = """Ответ не прошёл проверку: {error}
Верни только один JSON-объект строго по схеме из предыдущего сообщения, без markdown и текста вокруг.
Для полей без дословной цитаты ставь value = null и evidence = null."""


def analyze_messages(draft_text: str, industry: str) -> list[dict]:
    return [
        {"role": "system", "content": ANALYZE_SYSTEM},
        {"role": "user", "content": ANALYZE_USER.format(industry=industry, draft=draft_text)},
    ]


def build_messages(
    draft_text: str, industry: str, questions: list[Question], answers: list[Answer], prev_card: TaskCard,
) -> list[dict]:
    replies = {answer.question_id: answer.answer.strip() for answer in answers}
    qa = "\n".join(
        f"- [{question.field}] {question.text}\n  Ответ: {replies.get(question.id) or '(без ответа)'}"
        for question in questions
    )
    card = "\n".join(f"- {field}: {value}" for field, value in prev_card.model_dump().items() if value) or "(пусто)"
    return [
        {"role": "system", "content": BUILD_SYSTEM},
        {"role": "user", "content": BUILD_USER.format(industry=industry, draft=draft_text, qa=qa, card=card)},
    ]
